---
layout: page
title: 404
---

<script setup lang="ts">
import { computed } from 'vue'
import { useRoute, useData } from 'vitepress'

const { site } = useData()
const route     = useRoute()

// computed so it stays reactive during SPA navigation
const isEn = computed(() => route.path.includes('/en/'))
</script>

<div class="not-found">
  <div class="code">404</div>
  <h1>{{ isEn ? 'Page not found' : 'Страница не найдена' }}</h1>
  <p>{{ isEn ? 'The page you are looking for does not exist.' : 'Страница, которую вы ищете, не существует.' }}</p>
  <a :href="site.base">{{ isEn ? '← Back to home' : '← На главную' }}</a>
</div>

<style scoped>
.not-found {
  display: flex; flex-direction: column; align-items: center;
  justify-content: center; min-height: 60vh; text-align: center; gap: 16px;
}
.code {
  font-size: 96px; font-weight: 700; line-height: 1;
  background: linear-gradient(135deg, #54a0ff, #ff6b6b);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
}
h1 { font-size: 24px; margin: 0; }
p  { color: var(--vp-c-text-2); margin: 0; }
a  { color: var(--vp-c-brand); text-decoration: none; font-weight: 500; }
a:hover { text-decoration: underline; }
</style>
